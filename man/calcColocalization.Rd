% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colocalization_heatmap.R
\name{calcColocalization}
\alias{calcColocalization}
\title{Calculate colocalization values for all cell types across all slides}
\usage{
calcColocalization(
  srt,
  celltype.col,
  sd = 500,
  scale.factor = 10000,
  images = NULL,
  save.plots = FALSE,
  plot.dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{srt}{Seurat object with spatial images.}

\item{celltype.col}{Name of the metadata column containing cell type labels.}

\item{sd}{Standard deviation of the Gaussian field (controls the spatial
smoothing radius). Larger values produce smoother fields.}

\item{scale.factor}{Scale factor for field values (default 10000).}

\item{images}{Character vector of image names to process. If \code{NULL},
uses all images from \code{names(srt@images)}.}

\item{save.plots}{If \code{TRUE}, saves per-reference-celltype spatial value
plots via \code{\link{ImageDimPlot.values}}.}

\item{plot.dir}{Output directory for plots (used only when
\code{save.plots = TRUE}).}

\item{verbose}{If \code{TRUE}, prints progress messages.}
}
\value{
A \code{data.frame} with rows matching \code{colnames(srt)} and
  columns matching reference cell type names. Each value represents how
  strongly a cell colocalizes with the spatial distribution of the reference
  cell type. Cells in images where a reference type has fewer than 2 cells
  receive \code{NA}.
}
\description{
For each slide (image) and each reference cell type, creates a Gaussian
density field from the reference cell positions and evaluates it at every
cell position. Returns a data.frame of per-cell colocalization values that
can be fed into \code{\link{calcColocalizationHeatmap}}.
}
\examples{
\dontrun{
coloc <- calcColocalization(srt, celltype.col = "CellType", sd = 500)
}
}
